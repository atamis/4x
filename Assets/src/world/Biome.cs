using UnityEngine;
using System.Collections;

namespace game.world {
	/*
    class Biome {
		private Sprite sprite { get; set; }
		private float dropoff { get; set; }
		private int pass_cost { get; set; }
		private bool passable { get; set; }

        public Biome(string tex, float dropoff, int pass_cost, bool passable) {
            this.sprite = Resources.Load<Sprite>("Textures/" + tex);
            this.dropoff = dropoff;
            this.pass_cost = pass_cost;
            this.passable = passable;
        }
    }
    */
}
