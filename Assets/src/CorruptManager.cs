using UnityEngine;
using System.Collections;

// Handles the behavior for the Corruption
namespace game {
	public class CorruptManager : MonoBehaviour {

		GameManager parent;

		// Use this for initialization
		public void init (GameManager parent) {
			this.parent = parent;
		}
		
		// Update is called once per frame
		void Update () {
		
		}
	}
}
